using CSharpBasics;
using CSharpBasics.HelperClasses;

/*

// I have been given the task to write out a method that will iterate through the entire collection
// and print out the name of the item, how much it cost, what it's now worth, and calculate the profit

BaseballCard card = new BaseballCard
{
    Name = "Batter Dude",
    InitialCost = 10,
    CurrentValue = 100
};

FancyCookie cookie = new FancyCookie
{
    Name = "Chocolate Gold",
    InitialCost = 1000,
    CurrentValue = 7000,
    DaysBeforeExpiration = 10,
    PurchaseDate = DateTime.Now.AddDays(-20)
};

DisplayCollection(card);
DisplayCollection(cookie);
CheckExpired(cookie);



void DisplayCollection(ICollectable collection)
{
    Console.WriteLine($"You paid {collection.InitialCost:C} for {collection.Name}.");
    Console.WriteLine($"It's now worth {collection.CurrentValue:C}.");
    Console.WriteLine($"That's a profit of {collection.CalculateProfit():C}.");   
}

void CheckExpired(IEdible edible)
{
    if (edible.IsExpired())
    {
        Console.WriteLine("Throw it away");
    }
}

*/




Console.ReadLine();












